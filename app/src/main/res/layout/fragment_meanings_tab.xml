<?xml version="1.0" encoding="utf-8"?>
<FrameLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_color">

    <!-- Meanings ScrollView -->
    <ScrollView
        android:id="@+id/meaningsScrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">
        
        <TextView
            android:id="@+id/meaningsText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="1. to see; to look at\n2. to examine\n3. to look over"
            android:textSize="16sp"
            android:lineSpacingExtra="6dp"
            android:textColor="@color/text_primary_color" />
    
    </ScrollView>

    <!-- Empty State -->
    <LinearLayout
        android:id="@+id/emptyStateLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone">

        <ImageView
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@drawable/ic_book"
            android:alpha="0.3"
            android:layout_marginBottom="16dp"
            app:tint="@color/text_secondary_color" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No meanings found"
            android:textSize="18sp"
            android:textColor="@color/text_secondary_color"
            android:layout_marginBottom="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No definitions available for this word"
            android:textSize="14sp"
            android:textColor="@color/text_secondary_color"
            android:gravity="center"
            android:paddingHorizontal="32dp" />

    </LinearLayout>

</FrameLayout>